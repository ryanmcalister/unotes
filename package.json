{
  "name": "unotes-react",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.30.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:unotes.start"
  ],
  "main": "./ext-src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "unotes.start",
        "title": "Unotes"
      }
    ]
  },
  "private": true,
  "dependencies": {
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-scripts": "2.1.3",
    "tui-editor": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^10.12.18",
    "rewire": "^4.0.1",
    "typescript": "^3.2.4",
    "vscode": "^1.1.25"
  },
  "scripts": {
    "vscode:prepublish": "react-scripts build",
    "postinstall": "node ./node_modules/vscode/bin/install",
    "start": "react-scripts start",
    "build": "node build-non-split.js",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
